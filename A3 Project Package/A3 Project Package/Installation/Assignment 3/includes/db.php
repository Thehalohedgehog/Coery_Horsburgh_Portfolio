<?php
	/*This file will be used to connect to the database
	This feature was scrapped but this code will still be included in the package to show an attempt was made and for use in possible future iterations
	$hostservername = "localhost" ;
	$username = "root" ;
	$password = "root" ;
	$dbname = "SlimeArena" ;

	$dbconnection = new mysqli($hostservername, $username, $password, $dbname) ;

	if ($dbconnection -> connect_error) {
		die("Failed to connect to database<br>" . $dbconnection -> connect_error) ;
	}
	else {
		//echo "<h1>Connected!</h1>" ;
	}*/
?>